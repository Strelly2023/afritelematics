version: "3.9"

services:
  pilot-api:
    build:
      context: ../..
      dockerfile: afritech/docker/Dockerfile.pilot
    ports:
      - "8000:8000"
    environment:
      AFRIRIDE_PILOT_MODE: "controlled"
