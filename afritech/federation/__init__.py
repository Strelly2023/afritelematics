"""AfriTech federation layer.

This package intentionally exposes no static submodule imports. Federation is
constitutionally isolated as a speculative/non-operative surface, so validators
and tests load concrete modules dynamically when inspecting the surface.
"""

__all__: list[str] = []
