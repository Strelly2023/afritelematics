"""Frozen test placeholder for ecosystems.afriride.tests.observational.test_declaration_without_interpretation."""

from __future__ import annotations

import pytest


pytestmark = pytest.mark.skip(
    reason=(
        "FROZEN: this test surface is declared but has no admitted "
        "runtime behavior in the current epoch"
    )
)
