# Production Security Recovery Baseline

Assessment timestamp: 2026-07-22 (Australia/Melbourne)

- Branch: `feature/product-factory-enterprise-sdlc`
- Starting commit: `94f3b0afcbf6b3ec50aab4affdce6636d7dc3c60`
- Remote state at assessment: local `HEAD` matched `origin/feature/product-factory-enterprise-sdlc`
- Worktree: dirty because of pre-existing untracked evidence trees, `deployment-evidence/`, and root `csv.py`
- Evidence classification: `STRUCTURAL`
- Release decision: `NO-GO`

No production, provider, physical-device, failover, disaster-recovery, or external-signing activity was executed during this assessment.

| ID | Finding | Affected file | Route or service | Severity | Exploitability | Required remediation | Test required | Evidence required | Status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SEC-001 | Caller-controlled identity and role are signed into a token without authenticating the caller or resolving authority server-side. | `afriride_system/api/auth.py` | `POST /auth/token`, `POST /v1/auth/token` | Critical | Anonymous network caller can request a privileged role. | Replace with authenticated, authoritative identity/session issuance; isolate any developer helper from production. | Mounted-app negative token and claim-validation tests. | Security test report and route matrix bound to an immutable candidate. | OPEN |
| SEC-002 | Internal QA contract router is imported and mounted unconditionally by the active NovaRide entry point. | `afriride_system/api/main.py`, `afriride_system/api/internal_qa_contract_routes.py` | `/auth/login`, `/novaid/*`, `/novapay/*`, `/rides/*`, support and safety mocks | Critical | Anonymous caller receives successful mocked identity, wallet, ride, support, and safety responses. | Remove production import/mount; retain only under an explicit non-production gate and dedicated namespace. | Production-mode mounted-app reachability and startup-gate tests. | Negative security test report. | OPEN |
| SEC-003 | Authorization uses a path-prefix function where unmatched routes return no policy and are allowed. | `afriride_system/api/auth.py` | All mounted HTTP routes | Critical | New, misspelled, or unlisted routes silently become public. | Establish explicit public allowlist and deny routes lacking security metadata. | Route-inventory completeness plus positive and negative role tests. | Machine-readable route authorization matrix. | OPEN |
| SEC-004 | JWT claims omit issuer, audience, issued-at, not-before, token ID, tenant/session binding, and revocation checks. | `afriride_system/api/auth.py` | All bearer-authenticated NovaRide routes and sockets | High | Forged or replayed tokens may bypass intended trust and lifecycle boundaries if the signing secret is exposed or tokens outlive account/session state. | Validate signature, issuer, audience, time claims, identity/account/tenant membership, and active session/revocation state. | Invalid issuer/audience/signature, future/expired/revoked/unknown-session/disabled/deleted-user tests. | Security test report and audit event evidence. | OPEN |
| SEC-005 | Runtime signing secret falls back to a process-random value and production startup has no consolidated security assertions. | `afriride_system/api/auth.py`, `afriride_system/api/main.py` | NovaRide startup | High | Misconfiguration causes unstable authentication and insecure production settings are not rejected deterministically. | Add typed production configuration and fail startup on defaults, missing issuer/audience, QA/debug/dev issuance, unsafe URLs, or incomplete route policy. | Production startup assertion tests. | Redacted production environment validation. | OPEN |
| CFG-001 | Active application and deployment configuration includes development hosts/defaults and requires environment-specific classification. | `afriride_system/api/main.py`, `deploy/novaride/**`, production environment inputs | NovaRide API/deployment | High | Incorrect environment selection may expose development behavior or route traffic incorrectly. | Create authoritative validation and reject active production placeholders/localhost/insecure callback URLs. | Configuration validator tests. | Redacted deterministic validation report. | OPEN |
| GOV-001 | Multiple backend, Rider, Driver, Operator, NovaID, NovaPay, deployment, and CI candidates exist without one release-bound registry in this assessment. | repository-wide; notably `afriride_system`, `afritech/novaride_runtime`, `rider_app`, `driver_app`, `apps/novaride-operations` | Production build/deployment | High | CI or release tooling can certify a non-deployed implementation. | Inventory candidates, select canonical paths from actual deployment references, and validate alignment. | Registry/schema and deployment-reference tests. | Canonical runtime registry snapshot. | OPEN |
| GOV-002 | Root `csv.py` shadows Python's standard-library `csv` module and large untracked evidence trees are not governed. | `csv.py`, `artifacts/**`, `deployment-evidence/**` | Repository tooling and release evidence | Medium | Imports may resolve incorrectly; stale evidence may be mistaken for current certification. | Classify and govern without deleting user evidence; remove module shadowing safely. | Import-boundary and evidence-inventory validation. | Repository hygiene report. | OPEN |
| OPS-001 | Current commit lacks newly executed deployed PostgreSQL, Redis, Kafka, E2E, backup/restore, failover, multiprocess, load, and endurance evidence. | deployment environment and evidence directories | Operational certification | High | Operational behavior is unknown for this candidate. | Execute in an isolated environment where infrastructure is available and record exact outcomes. | Deployed integration/resilience/performance suites. | Commit-bound operational reports. | NOT_EXECUTED |
| MOB-001 | Physical Android/iOS devices and current commit-bound signed artifacts have not been exercised. | `rider_app`, `driver_app` | Mobile release | High | Real-device security and lifecycle behavior is unverified. | Build canonical candidates and execute the required physical-device matrix. | Physical-device scenarios. | Device, OS, artifact hash, tester, logs, and results. | NOT_EXECUTED |
| EXT-001 | Provider execution, authorized signing material, and external approvals are not established by repository inspection. | external systems | Provider/signing/approval | High | Release cannot meet mandatory certification criteria. | Execute with authorized credentials and independent approval. | Provider and signature verification. | `LIVE_PROVIDER_TESTED` and `EXTERNALLY_SIGNED` evidence where genuinely performed. | BLOCKED |

## Initial runtime observations

- Active NovaRide deployment command: `uvicorn afriride_system.api.main:app` in `deploy/novaride/docker-compose.realtime.yml`.
- Candidate backend code also exists under `afritech/novaride_runtime` and other legacy AfriRide trees; canonical selection remains open.
- Rider and Driver candidates include root `rider_app` and `driver_app`, plus older Expo/Flutter implementations.
- Operator candidates include `apps/novaride-operations`, `novaride_operator_app`, portals, and legacy operator modules.
- NovaID and NovaPay integrations exist in `afritech` and are mounted or consumed by multiple surfaces; canonical binding remains open.
- Production workflow candidate: `.github/workflows/novaride-production-operated-gates.yml`.

The release status remains `NO-GO` until every mandatory criterion is executed and passed against one clean immutable commit.
