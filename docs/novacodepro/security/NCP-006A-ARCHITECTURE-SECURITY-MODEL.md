# NCP-006A Architecture Security Model

Architecture Studio is governed by:

- authenticated server-side tenant context
- architecture role and permission checks
- workspace-scoped model resolution
- cross-tenant rejection
- approval and baseline gating
- audited mutation events

The architecture model may be drafted from AI execution evidence, but only with explicit provenance and governed permissions. Traceability links are validated against existing resources before persistence.
