# Controlled Pilot Devices

The controlled pilot uses 40 approved devices.

Device policy:
- Every device is registered.
- Every device is trusted.
- Every device is bound to one approved user.
- The test suite does not require one device per user.

Reference:
- `pilot/controlled_pilot/approved_devices.json`
