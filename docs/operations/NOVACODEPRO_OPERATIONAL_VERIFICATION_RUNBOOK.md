# NovaCodePro Operational Verification Runbook

Run `python3 scripts/novacodepro/run_operational_verification_v1.py` with the
target environment and release. Production runs must use real configured
credentials and signing providers.
