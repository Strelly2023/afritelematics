import assert from"node:assert/strict";import{readFileSync}from"node:fs";import test from"node:test";const source=readFileSync(new URL("../src/design/DeliveryGovernanceStudio.jsx",import.meta.url),"utf8");test("delivery studio binds reviews and approvals to exact artifact versions",()=>{assert.match(source,/reviewed_version:selected\.version/);assert.match(source,/Approve pending version/);assert.match(source,/Any later edit requires a new review/);});test("handoff covers specifications contracts accessibility APIs acceptance and tests",()=>{for(const value of ["Screen specifications","Component specifications","Accessibility requirements","API bindings","Data & event contracts","Acceptance criteria","Test requirements","Version references"])assert.match(source,new RegExp(value));for(const framework of ["React","Vue","Angular","Flutter","SwiftUI","Jetpack Compose","Tailwind"])assert.match(source,new RegExp(framework));});test("exports are tenant governed and checksum recorded",()=>{assert.match(source,/crypto\.subtle\.digest\("SHA-256"/);assert.match(source,/storage_reference/);assert.match(source,/approval_state/);for(const format of ["PNG","SVG","PDF","Storybook","Prototype package","OpenAPI-linked documentation"])assert.match(source,new RegExp(format));});
