apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: novaride-api
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: novaride-api
  minReplicas: 6
  maxReplicas: 30
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
