global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - /etc/prometheus/rules/alert_rules.yml

scrape_configs:
  - job_name: afripay-api
    metrics_path: /api/afripay/metrics/prometheus
    scheme: http
    static_configs:
      - targets:
          - afripay-api:8000
