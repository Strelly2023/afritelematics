# NovaID WebAuthn metrics certificate

LOCAL VERIFICATION ONLY — NOT FIDO CERTIFICATION — NOT PRODUCTION CERTIFICATION — NOT GA CERTIFICATION.

Result: **PASS locally for instrumentation**. Registration/authentication success/failure and credential lifecycle counters use the existing Prometheus-compatible exporter. Backend scrape/dashboard/alert verification is outside local evidence.
