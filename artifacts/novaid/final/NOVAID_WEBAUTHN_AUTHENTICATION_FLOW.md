# NovaID WebAuthn authentication flow

Identity-bound or discoverable options persist a hashed challenge. Verification validates RP ID, origin, presence, verification, credential ownership, signature, and counter. Success returns the tenant/identity and `PHISHING_RESISTANT` strength; access/refresh token issuance is not yet integrated.
