# Phase 6E Redis failure certificate

LOCAL VERIFICATION ONLY — NOT PHYSICAL-DEVICE CERTIFICATION — NOT FIDO CERTIFICATION — NOT PRODUCTION CERTIFICATION — NOT GA CERTIFICATION.

Retry/dead-letter and reconnect code exists. Required/optional outage, restart, flush, rebuild, and backlog recovery matrix was not executed after runtime wiring.
