# Password lifecycle

Creation and verification use the Phase 1 scrypt implementation and normalized credential tables. Change, reset, history, compromise, transparent rehash, security-version increment, and session invalidation services remain incomplete.
