# NovaID passkey model

Passkeys are discoverable WebAuthn credentials. Stored metadata includes credential ID, COSE public key, AAGUID, transports, device type, backup eligibility/state, user-verification state, sign count, tenant, identity, and lifecycle status. Backup metadata distinguishes multi-device/synced indications from single-device credentials but does not certify vendor sync behavior.
