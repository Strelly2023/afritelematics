# PostgreSQL implementation

The normalized PostgreSQL DDL and isolated PostgreSQL 16 compose service exist. Concrete PostgreSQL repositories and unit of work remain incomplete because `psycopg` and a running server were unavailable.
