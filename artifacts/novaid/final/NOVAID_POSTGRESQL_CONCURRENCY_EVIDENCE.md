# PostgreSQL concurrency evidence

Actual PostgreSQL locking and optimistic-conflict paths were executed. The full independent-worker OTP, refresh, reset, logout and lockout race matrix was not executed and remains PARTIAL.
