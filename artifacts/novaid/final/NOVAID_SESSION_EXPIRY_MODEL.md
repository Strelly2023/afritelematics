# Session expiry model

On-access/sweeper-compatible expiration moves active, pending-MFA or step-up sessions past absolute expiry to EXPIRED. Additional distinct idle and step-up timestamps remain a future schema refinement.
