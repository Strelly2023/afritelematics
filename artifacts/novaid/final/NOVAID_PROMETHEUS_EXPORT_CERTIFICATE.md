# NovaID Prometheus export certificate

LOCAL VERIFICATION ONLY — NOT PRODUCTION CERTIFICATION — NOT GA CERTIFICATION.

The `/metrics` endpoint exports Prometheus-compatible low-cardinality counters. Tests prove counts render correctly and identity/password values are absent. No external Prometheus server ingestion occurred.
