# NovaID trace export certificate

LOCAL VERIFICATION ONLY — NOT PRODUCTION CERTIFICATION — NOT GA CERTIFICATION.

Structured JSONL span export passes locally and retains span names/attributes without secrets. This is not OpenTelemetry SDK/OTLP collector ingestion; `OBSERVABILITY_BACKEND_VERIFIED` remains NO.
