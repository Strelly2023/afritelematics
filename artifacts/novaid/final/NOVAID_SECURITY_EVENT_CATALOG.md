# NovaID security-event catalog

The envelope records event/severity, tenant, actor, subject, correlation/request identifiers, timestamps, outcome, reasons, metadata and schema version. Metadata keys for passwords, OTPs, access/refresh tokens, private keys and recovery secrets are rejected. Phase 2 tests `IDENTITY_ACTIVATED` and `AUTHENTICATION_FAILED`; the full event list remains an integration requirement.
