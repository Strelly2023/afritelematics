# NovaID session lifecycle certificate

LOCAL VERIFICATION ONLY — NOT PRODUCTION CERTIFICATION — NOT GA CERTIFICATION.

Verified states: PENDING_MFA, ACTIVE, STEP_UP_REQUIRED, LOCKED, REVOKED, EXPIRED, COMPROMISED. Verified operations include activation, listing, touch implementation, step-up transitions, lock/unlock, selected/all revocation, classified expiry sweep, on-access/refresh expiry, and terminal compromise containment. Expiry metrics and `novaid.session.expire` span are tested.
