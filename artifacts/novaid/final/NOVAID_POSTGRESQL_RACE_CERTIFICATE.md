# NovaID PostgreSQL race certificate

LOCAL VERIFICATION ONLY — NOT PRODUCTION CERTIFICATION — NOT GA CERTIFICATION.

Not certified. Worker count 1; race iterations 0; deadlocks 0; serialization failures 0. Row-lock and optimistic-conflict tests pass, but the required multi-worker matrix was not executed.
