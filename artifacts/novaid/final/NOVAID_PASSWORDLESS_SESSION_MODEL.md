# Passwordless session model

A verified discoverable assertion resolves an active tenant identity and membership, advances the credential counter, creates an ACTIVE `PHISHING_RESISTANT` session, creates a refresh family and one-time hashed refresh token, and allows the API to issue an access token. Password and OTP are not involved.
