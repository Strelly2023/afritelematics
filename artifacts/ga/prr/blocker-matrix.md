# GA blocker matrix

The authoritative machine-readable blocker list is `blocker-matrix.json`.
Every listed gate remains mandatory and the release decision is `NO-GO`.

No blocked gate has been converted to a pass using source review, historical
evidence, mocks, emulators, unsigned checksums, or container health alone.
