# NovaRide event catalog

Material events in this branch include ride lifecycle, payment, outbox/replay, identity, security, and operations-control events.
The repository already contains event schema, hash, replay, and outbox infrastructure under `afritech/novaride_runtime/events` and NovaID persistence/event code under `afritech/novaid`.
