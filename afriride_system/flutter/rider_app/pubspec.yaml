name: afriride_rider_pilot
description: Minimal AfriRide rider pilot client.
publish_to: "none"

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  crypto: ^3.0.3
  http: ^1.2.0
  web_socket_channel: ^2.4.0
